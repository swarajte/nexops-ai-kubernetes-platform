# Daily Progress — 2026-08-22

## Date
2026-08-22

## Current stage
Stage 3 — Kubernetes + Helm (COMPLETED)

## Work completed today
- Implemented Stage 3 on the POC Kubernetes cluster.
- Created raw manifests under `k8s/` (Namespace, ConfigMap, Deployments, Services, probes, resource limits).
- Built images on POC (`nexops/frontend:v1`, `nexops/orders-api:v1`, `nexops/payment-api:v1`) and imported them into containerd.
- Applied raw YAML, verified frontend → orders-api → payment-api.
- Packaged the same resources as Helm chart `helm/nexops`.
- Replaced the raw install with `helm install nexops ./helm/nexops -n nexops --create-namespace`.
- Verified `helm upgrade` and port-forward access.

## Tests
- Raw YAML: all 3 pods Ready; in-cluster health + order confirmed
- Frontend `/api/orders` through nginx: confirmed + payment success
- Helm install: pods Ready
- Helm upgrade: revision 2
- Port-forward `svc/frontend` : health + order confirmed

## Problems encountered
- Docker image builds on POC need the environment proxy (not committed).
- Kubernetes uses containerd, so Docker-built images were imported with `ctr -n k8s.io images import`.

## Decisions made
- Namespace: `nexops`
- Access: ClusterIP + `kubectl port-forward` (no NodePort)
- Keep both `k8s/` and `helm/nexops`
- Replace raw YAML deploy with Helm after the raw path worked
- No Secrets, no Ingress, no monitoring

## Next step
- Stage 4 — Failure Simulation (not started)
