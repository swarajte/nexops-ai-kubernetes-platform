# Daily Progress — 2026-08-22

## Date
2026-08-22

## Current stage
Stage 5 — Monitoring (COMPLETED)

## Work completed today
### Stage 3 (earlier)
- Raw Kubernetes YAML, then Helm on POC namespace `nexops`.

### Stage 4
- Added payment-api failure injection (`/ready`, `/fail/*`, `FAILURE_MODE` env).
- Split probes: liveness `/health`, readiness `/ready`.
- Helm overlays in `helm/nexops/failures/` for OOM, crash, image pull, ready, CPU, memory, slow, errors.
- Rebuilt `nexops/payment-api:v2`, imported to containerd, `helm upgrade`.
- Documented what / why / how / recover in `docs/COMMANDS.md`.

### Stage 5
- Added Prometheus `/metrics` (prometheus-fastapi-instrumentator) and request logs on orders-api and payment-api.
- ServiceMonitors with `release: prometheus` so the existing kube-prometheus-stack scrapes NexOps.
- Grafana dashboard ConfigMap (folder NexOps) + Loki datasource ConfigMap (sidecar watches all namespaces).
- Installed Loki + Promtail (`grafana/loki-stack`) in `nexops-monitoring`; Promtail limited to namespace `nexops`.
- Did **not** install a second Prometheus/Grafana (cluster already has them in `monitoring`).
- Images: `nexops/orders-api:v2`, `nexops/payment-api:v3`.

## Tests
- payment-api / orders-api pytest including `/metrics`
- Cluster: ServiceMonitor targets, Grafana dashboard, Loki query `{namespace="nexops"}`

## Problems encountered
- `IfNotPresent` would keep serving old `v1` image; Stage 4 uses tag **v2**.
- A second kube-prometheus-stack would collide with existing CRDs/operator selectors.

## Decisions made
- Failures only on payment-api (first incident target).
- Prefer Helm overlay for K8s-level symptoms; HTTP for runtime symptoms.
- Reuse cluster Prometheus/Grafana; add Loki ourselves.
- ServiceMonitor label must be `release: prometheus`.

## Next step
- Stage 6 — Incident Detector
