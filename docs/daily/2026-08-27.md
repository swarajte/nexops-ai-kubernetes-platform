# Daily Progress — 2026-08-27

## Current stage
Stage 9 allowlisted remediation completed and verified on the POC.

## Work completed
- Added FastAPI remediation service with SQLite decision/audit history.
- Added current-incident, incident/analysis, problem/action, target, and memory-cap validation.
- Implemented four allowlisted Deployment patches for `payment-api`.
- Connected `/ops` approval, rejection, progress, errors, and recovery state to the service.
- Added nginx/Vite proxying and Helm Deployment, ServiceAccount, scoped Role, RoleBinding, Service, probes, and non-root settings.
- Deployed `nexops/remediation:v1`, `nexops/frontend:v3`, chart `0.6.0`.

## Verification
- Remediation tests: 15 passed.
- Frontend: 3 Vitest tests passed; TypeScript and production build passed.
- Helm lint and template passed.
- POC acceptance: HighErrorRate incident `ccb4f8db-…` and analysis `2e9a5855-…` produced `reset_failure_mode`; approval persisted as remediation `d6be2130-…`; status reached `succeeded`; incident reached `RESOLVED`; app failure status returned healthy.
- OOM acceptance: incident `d893a7ef-…` produced `increase_memory` 32Mi→64Mi; the service also cleared the infinite OOM injector, verified recovery, and the incident reached `RESOLVED`.
- Helm 4 reconciliation after direct patches requires `--reset-values --force-conflicts`; verified back at healthy 128Mi baseline.
- Final POC: Helm revision 95, OPEN `[]`, payment-api healthy.

## Security finding for Stage 10
The chart Role is restricted to get/patch `deployment/payment-api`, but an existing operator ClusterRoleBinding grants all service accounts broad Deployment writes. Kubernetes RBAC is additive. Stage 10 must narrow that cluster-level grant; Stage 9's server-side allowlist remains enforced.

## Stage plan (docs only, not started)
- Stage 11 — CI with clean Docker images: GitHub Actions tests + proxy-free `docker build`. No ECR, no Helm, no POC pod updates.
- Stage 12 — AWS + ECR + EKS + Helm (IAC + CD): first run of NexOps outside the corporate POC.
- Former standalone “AWS/EKS” stage is part of Stage 12. Following stages: 13 production improvements, 14 demo, 15 reverse engineering.
